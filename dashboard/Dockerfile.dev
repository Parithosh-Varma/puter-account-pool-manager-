FROM node:20-alpine

WORKDIR /app

COPY package.json tsconfig.json vite.config.ts ./
COPY index.html ./
RUN npm ci --ignore-scripts

COPY src/ ./src/

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
